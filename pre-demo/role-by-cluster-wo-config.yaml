kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: armenia
  name: namespace-reader
  annotations:
    configsync.gke.io/cluster-name-selector: fleet-cluster-0001,non-fleet-project-cluster-01,autopilot-cluster-1
rules:
- apiGroups: [""]
  resources: ["namespaces"]
  verbs: ["get", "watch", "list"]
