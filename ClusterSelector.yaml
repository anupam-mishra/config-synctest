kind: ClusterSelector
apiVersion: configmanagement.gke.io/v1
metadata:
  name: cluster-selector
spec:
  selector:
    matchExpressions:
    - key: env
      operator: In
      values:
      - test
    - key: location
      operator: In
      values:
      - usa
